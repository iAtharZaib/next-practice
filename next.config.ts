import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // experimental: {
  //   ppr: 'incremental'
  // } for experimental PPR support in Next.js 14
  /* config options here */
};

export default nextConfig;
